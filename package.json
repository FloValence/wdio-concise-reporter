{
  "name": "wdio-concise-reporter",
  "version": "0.1.2",
  "description": "Reporter that is concise",
  "main": "build/reporter.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FloValence/wdio-concise-reporter.git"
  },
  "directories": {
    "lib": "./lib"
  },
  "scripts": {
    "build": "rimraf build && babel lib -d build",
    "watch": "babel lib -d build --watch",
    "prepublish": "npm prune && npm run build"
  },
  "keywords": [
    "wdio",
    "webdriverio",
    "concise",
    "reporter"
  ],
  "author": "Florian Valence <florian.m.valence@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-es2015": "^6.9.0",
    "rimraf": "^2.5.4"
  }
}
